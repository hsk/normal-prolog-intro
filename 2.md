---
title: "2. 変数と条件分岐"
---

## 2. 変数と条件分岐

### 論理変数：「箱」ではなく「未知の空欄」

Prologの変数は一度中身が決まったら**書き換えられない空欄（プレースホルダ）**です。

```prolog
% var.pl
:- X = 1, writeln(X).
:- halt.
```

```sh
$ swipl var.pl
1
```

`X = 1` は代入ではなく「Xという空欄に1を当てはめる（単一化する）」という意味です。詳しくは3章で扱います。変数は大文字またはアンダースコアで始まり、一度埋まった空欄には別の値を当てはめ直せません。

```prolog
% var2.pl
:- X = 1, X = 2, writeln(X).
:- halt.
```

```sh
$ swipl var2.pl
Warning: /path/to/var2.pl:2:
Warning:    Goal (directive) failed: user:(_1234=1,_1234=2,writeln(_1234))
```

`X = 1`のあとに`X = 2`を試みると失敗し、`writeln(X)`まで到達しないため標準出力には何も出ません。代わりに、ディレクティブが失敗したことを知らせる警告が標準エラーに出力されます。

### アトム：記号としての値

```prolog
% atom.pl
:- X = abc, writeln(X).
:- Y = 'hello world', writeln(Y).
:- halt.
```

```sh
$ swipl atom.pl
abc
hello world
```

`abc`のように英小文字で始まる記号はそのまま**アトム**になります。空白や記号を含む場合は`'hello world'`のようにクォートで囲みます。
1章の `'hello world'` もこのアトムでした。
Prologのアトムは同じアドレスが割り当てられるため文字列より高速に動作します。

### リスト

```prolog
% list.pl
:- L = [1, 2, 3], writeln(L).
:- halt.
```

```sh
$ swipl list.pl
[1,2,3]
```

詳しい操作（先頭・残りへの分解や`append`など）は4章でまとめて扱います。

### 条件分岐：`Cond -> Then ; Else`

```prolog
% if.pl
:- X = 4, ( 0 is X mod 2 -> writeln(even) ; writeln(odd) ).
:- X = 3, ( 0 is X mod 2 -> writeln(even) ; writeln(odd) ).
:- halt.
```

```sh
$ swipl if.pl
even
odd
```

`Cond -> Then ; Else` は「`Cond`が証明できれば`Then`を、できなければ`Else`を実行する」という意味です。
`( ... )`で全体を囲む必要は必ずしも必要ではありませんが、優先順位がわからなくならないように括弧で囲むと間違いが減るでしょう。

```prolog
% if2.pl
:- X = 4, ( 0 is X mod 2, writeln(even) ; writeln(odd) ).
:- X = 3, ( 0 is X mod 2, writeln(even) ; writeln(odd) ).
:- halt.
```

このように書いても同様に動きます。

---

[← 1. はじめの一歩：Hello World](1.md) | [3. ループの書き方 →](3.md)

[目次に戻る](README.md)
