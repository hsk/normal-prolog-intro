---
title: "1. はじめの一歩：Hello World"
---

## 1. はじめの一歩：Hello World

### インストール：SWI-Prologの導入手順

Prologの処理系にはいくつか種類がありますが、この入門では事実上の標準である **SWI-Prolog** を使います。

- macOS: `brew install swi-prolog`
- Windows: WSL2（Ubuntuなど）を導入した上で、Linux向けの手順に従う
- Linux（WSL2内も含む）:
  `sudo apt install swi-prolog` または
  [公式サイト](https://www.swi-prolog.org/download/stable) の手順に従う

インストールが終わったら、ターミナルで対話環境（トップレベル）を起動できるか確認します。

```sh
$ swipl
Welcome to SWI-Prolog (threaded, 64 bits, version 9.x.x)
...
?- 
```

`?-` はPythonの`>>>`にあたるプロンプトです。試しに質問してみましょう。

```prolog
?- X is 1 + 1.
X = 2.

?- halt.
```

`X is 1 + 1.` は「Xを1+1という式の解として求めよ」という**問い合わせ（クエリ）**です。`halt.` はPythonの`exit()`にあたり、これで終了します。

### 最初の述語：`hello_world`

Pythonならこう書きます。

```python
print("hello world")
```

Prologでは「関数」ではなく**述語（predicate）**を書きます。ファイル `hello.pl` を作成します。

```prolog
% hello.pl
:- writeln('hello world').
```

- `writeln('hello world')` は文字列を1行出力する組み込み述語です。
- `'hello world'` のようにクォートで囲まれた文字列は**アトム**（記号定数）です。詳しくは2章で扱います。
- `:-` で始まる行は**ディレクティブ**と呼ばれ、ファイルをロードした瞬間に実行されます。

実行するには、ターミナルからスクリプトとして起動します。

```sh
$ swipl hello.pl
hello world
?-
```

このままだと対話環境が起動したままになるので、`halt`を呼ぶディレクティブで終了させます。

```prolog
% hello.pl
:- writeln('hello world').
:- halt.
```

```sh
$ swipl hello.pl
hello world
$
```

ここで使った`,`（カンマ）についても触れておきます。

```prolog
% comma.pl
:- X is 1 + 1, writeln(X).
:- halt.
```

```sh
$ swipl comma.pl
2
```

`X is 1 + 1, writeln(X)` のように複数のゴールを`,`でつなげると、「左のゴールが成功したら、続けて右のゴールも実行する」という意味になります。
「文の区切り」というより、論理学の**論理積（AND）**そのもので、「`X is 1 + 1`が成り立ち、かつ`writeln(X)`も成り立つ」ことを1行で表しています。

逆に言うと、左側が失敗すると、`,`の右側は実行されません。

```prolog
% comma_fail.pl
:- X is 1 + 1, X = 3, writeln(X).
:- halt.
```

```sh
$ swipl comma_fail.pl
Warning: /Users/hiroshisakurai/Documents/prolog/prolog-intro/comma_fail.pl:2:
Warning:    Goal (directive) failed: user:(_13132 is 1+1,_13132=3,writeln(_13132))
```

`X is 1 + 1` の時点でXは2になっているので、続く`X = 3`は「2と3を単一化できるか」を試みて失敗します。`,`より右にある`writeln(X)`まで到達しないため、何も出力されません。
同じ理由で、`:- writeln('hello world'), halt.`と1つのディレクティブにまとめると、`writeln`が失敗した場合に`halt`まで実行されなくなってしまいます。
ですから`:- writeln('hello world').`と`:- halt.`を分けて書くと良いでしょう。

多くのProlog入門書では、`main`という述語を定義してから`initialization(main).`で呼び出す書き方を勧めます。
しかし SWI-Prolog では `:- ...` だけで動かす方が手軽なので、この形で話を進めます。

### コメント

`% hello.pl`のように`%`から行末まではコメントになります（1行コメント）。複数行にまたがるコメントは`/* ... */`で囲みます。

```prolog
% これは1行コメント
/*
  これは
  複数行コメント
*/
:- writeln('hello world').
:- halt.
```

まとめると、この章で押さえておきたいことは次の5点です。

1. Prologは対話環境（トップレベル）が言語体験の中心であり、ファイルをスクリプトとして実行することもできる。
2. `:-`で始まるディレクティブは、ファイルがロードされた瞬間にその場で実行される。
3. `:- halt.` で終わらせる。
4. `,`はゴールとゴールをつなぐ論理積（AND）で、左が成功して初めて右が実行される。
5. コメントは1行コメント `%` と 複数行コメント `/**/` を使います。

---

[2. 変数と条件分岐 →](2.md)

[目次に戻る](README.md)
