---
title: "6. パターンマッチによる分岐（switch文）"
---

## 6. パターンマッチによる分岐（switch文）

### 節の記述：複数の節による条件分岐

2章の`Cond -> Then ; Else`を使えば、switch文のような多分岐も一応書けます。

```prolog
% sign_if.pl
sign(X, S) :-
    ( X > 0 -> S = positive
    ; X < 0 -> S = negative
    ; S = zero
    ).

:- sign(3, S), writeln(S).
:- sign(-1, S), writeln(S).
:- sign(0, S), writeln(S).
:- halt.
```

```sh
$ swipl sign_if.pl
positive
negative
zero
```

書けはしますが、`;`が増えるほどネストと括弧が深くなり、Haskellで`case`の代わりに関数の等式を並べて書くのに近い読みにくさが出てきます。Prologらしいのは、1つの述語の中に`;`を詰め込むのではなく、**引数のパターンごとに節を分けて書く**方法です。

```prolog
% sign.pl
sign(0, zero).
sign(X, positive) :- X > 0.
sign(X, negative) :- X < 0.

:- sign(3, S), writeln(S).
:- sign(-1, S), writeln(S).
:- sign(0, S), writeln(S).
:- halt.
```

```sh
$ swipl sign.pl
positive
negative
zero
```

`sign(0, zero).`は「0番目のパターン」を第1引数の**値そのもの**でマッチさせ、残りの2節は第1引数を変数`X`で受けて条件を絞り込んでいます。呼び出すたびに上から順に節を試し、パターン（と条件）が一致した節だけが成功します。Haskellで

```haskell
sign 0 = "zero"
sign x | x > 0 = "positive"
       | x < 0 = "negative"
```

のように関数の定義を分けて書くのと同じ発想で、Prologでは述語の**節**を分けて書きます。

### 制御：カット演算子 `!` によるバックトラックの停止

先ほどの`sign/2`には、実は問題があります。

```prolog
% sign_bug.pl
sign(0, zero).
sign(X, positive) :- X > 0.
sign(X, negative) :- X < 0.

:- findall(S, sign(0, S), List), writeln(List).
:- halt.
```

```sh
$ swipl sign_bug.pl
[zero]
```

これは一見正しく見えますが、`sign(0, positive)`のように第2引数まで具体的な値を渡して問い合わせると、想定していない挙動が起こり得ます。

```prolog
% sign_bug2.pl
sign(0, zero).
sign(X, positive) :- X > 0.
sign(X, negative) :- X < 0.

:- ( sign(0, positive) -> writeln(yes) ; writeln(no) ).
:- halt.
```

```sh
$ swipl sign_bug2.pl
no
```

この例はたまたま正しく`no`になりますが、`0`が`zero`だけでなく他の節にもマッチしうるような定義だと、1つの問い合わせに対して複数の節が成功してしまい、`findall/3`で意図しない解まで集まってしまうことがあります。「この節が一致したら、もう他の節は試さなくていい」と明示したいときに使うのが**カット演算子`!`**です。

```prolog
% sign_cut.pl
sign(0, zero) :- !.
sign(X, positive) :- X > 0, !.
sign(X, negative) :- X < 0.

:- findall(S, sign(0, S), List), writeln(List).
:- halt.
```

```sh
$ swipl sign_cut.pl
[zero]
```

`!`はそこまでの選択（どの節を選んだか、`;`のどちらを選んだか）を確定させ、バックトラックしてもそこより前には戻らせない、という働きをします。
`sign(0, zero) :- !.`の`!`は、「第1引数が0なら`zero`に決まりで、`positive`や`negative`の節は二度と試さない」という意味になり、switch文の`break`に近い役割を果たします。

### 否定：`\+`（failによる証明）の用法

「〜でない」ことを表したいときは`\+`（否定）を使います。

```prolog
% not.pl
even(X) :- 0 is X mod 2.

:- ( \+ even(3) -> writeln(yes) ; writeln(no) ).
:- halt.
```

```sh
$ swipl not.pl
yes
```

`\+ Goal` は「`Goal`が証明できない」ことを表します。
中身は「`Goal`を試してみて、成功したら全体は失敗、失敗したら全体は成功」という単純な仕組みで、これを**failによる証明（negation as failure）**と呼びます。
「存在しないことを証明した」わけではなく、「今持っている事実とルールの範囲では証明できなかった」ことを成功とみなしている点に注意してください。
たとえば`extends/2`のデータベースに登録し忘れがあっても、`\+ extends(fish, animal)`は「fishはanimalを継承しない」ではなく「今のところそう書かれていないから継承していないとみなす」という意味でしか成り立ちません。

---

[← 5. 述語（述語論理としての関数）を作ろう](5.md) | [7. リスト操作の基礎 →](7.md)

[目次に戻る](README.md)
