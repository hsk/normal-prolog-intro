---
title: "6. 述語（述語論理としての関数）を作ろう"
---

## 6. 述語（述語論理としての関数）を作ろう

ここまでの章では、`writeln`や`between`、`append`のように**用意されている述語を呼ぶ**だけでした。この章では、いよいよ自分で述語を**作る**方法を学びます。

### 設計思想：戻り値ではなく「引数への書き込み」

Prologは**論理型言語**なので、プログラムは論理式の集まりでできています。論理式が返せるのは「成り立つ（true）」か「成り立たない（false）」かだけなので、Prologの`f(...)`は関数ではなく**述語**と呼ばれ、戻り値として値を持ち帰ることができません。

それでも計算結果を持ち帰りたい場面は当然あります。そこでC言語のポインタやC++の参照のように、「結果を書き込む場所」を引数として渡すという発想を使います。Prologの変数は8章で扱う**単一化変数**なので、まだ何にも当てはまっていない未束縛の変数を渡しておけば、述語の中でその変数に値が当てはめられ、呼び出し側からもその値が見える、という仕組みで「値を返す」ことを実現します。

```prolog
% double.pl
double(X, Y) :- Y is X * 2.

:- double(3, Result), writeln(Result).
:- halt.
```

```sh
$ swipl double.pl
6
```

`double(X, Y)` は「Xの2倍がYである」という**関係**を表す述語です。「Xを渡したらYが返ってくる関数」ではなく、「XとYの間にこういう関係が成り立つ」という事実を述べていると考えてください。呼び出し側は、結果を受け取りたい変数（ここでは`Result`）を第2引数の場所にそのまま渡します。

### データベース：事実（Fact）の記述

述語の一番シンプルな形は、ルールを持たない**事実（Fact）**です。

```prolog
% fact.pl
extends(dog, animal).
extends(cat, animal).
extends(animal, object).

:- ( extends(dog, animal) -> writeln(yes) ; writeln(no) ).
:- halt.
```

```sh
$ swipl fact.pl
yes
```

`extends(dog, animal).` は「dogはanimalを継承する（extendsする）」という事実をそのままデータベースに登録しているだけです。
複数の事実を並べておけば、`extends(dog, animal)`のような問い合わせに対して「その事実がデータベースにあるかどうか」を照合して答えてくれます。

### ロジック：ルール（Rule）の定義

事実だけでなく、「こういう条件が満たされたら成り立つ」という**ルール**も書けます。

```prolog
% rule.pl
extends(dog, animal).
extends(cat, animal).
extends(animal, object).

is_a(X, Y) :- extends(X, Z), extends(Z, Y).

:- ( is_a(dog, object) -> writeln(yes) ; writeln(no) ).
:- halt.
```

```sh
$ swipl rule.pl
yes
```

`is_a(X, Y) :- extends(X, Z), extends(Z, Y).` は「XがZをextendsし、ZがYをextendsするなら、Xは（間接的に）Yの一種である」というルールです。
`:-`は「ならば」、`,`は「かつ」を表します。
実際にdogはanimalをextendsし、animalはobjectをextendsしているので、`is_a(dog, object)`が成り立ちます。

ただし、この`is_a/2`は「孫」にあたる2段階の継承しか判定できません。継承がもっと深くなっても対応できるようにしたい場合、「XがYをextendsする、またはXがZをextendsしていてZがYの一種である」という再帰的なルールを素直に書きたくなります。

```prolog
% is_a_bad.pl
extends(dog, animal).
extends(animal, object).

is_a(X, Y) :- extends(X, Y).
is_a(X, Y) :- is_a(X, Z), is_a(Z, Y).

:- ( is_a(dog, object) -> writeln(yes) ; writeln(no) ).
:- halt.
```

これは論理式としては正しく見えますが、実行すると答えが返ってこず無限ループに陥ります（実際には途中でスタックが尽きてエラーになります）。
原因は2つ目の節`is_a(X, Y) :- is_a(X, Z), is_a(Z, Y).`の最初のゴールが、XもZも決まっていない状態で`is_a(X, Z)`という自分自身をそのまま呼び出してしまう点です。これは構文解析の文法規則が左再帰（自分自身を先頭で呼ぶ）だと無限ループするのと同じ現象で、モーダスポンネス（「AならばB、Aが成り立つ、ゆえにBが成り立つ」という推論）を機械的に繰り返しているだけでは、どこまで手を伸ばしても終わりが来ません。

これを避けるには、必ず`extends/2`という「実際にデータベースを1歩たどるゴール」を先に評価し、Zの値を先に決めてから再帰するように書き換えます。

```prolog
% is_a_good.pl
extends(dog, animal).
extends(animal, object).

is_a(X, Y) :- extends(X, Y).
is_a(X, Y) :- extends(X, Z), is_a(Z, Y).

:- ( is_a(dog, object) -> writeln(yes) ; writeln(no) ).
:- halt.
```

```sh
$ swipl is_a_good.pl
yes
```

`is_a(X, Y) :- extends(X, Z), is_a(Z, Y).` の順番なら、まず`extends(X, Z)`でZが具体的な値（`animal`）に決まります。
そのあと`is_a(Z, Y)`を呼ぶので、再帰のたびに継承の階層を1段ずつ登っていき、いつか必ず終わります。
同じ論理式に見えても、節の中でゴールをどの順番に並べるかによって止まるか止まらないかが変わります。

### 特徴：複数の解を返す述語の仕組み

述語は同じ名前で複数の節（事実やルール）を持てます。呼び出すと、上から順に一致する節を試し、バックトラックすればすべての解を得られます。

```prolog
% multi.pl
extends(dog, animal).
extends(cat, animal).
extends(animal, object).

:- findall(X, extends(X, animal), List), writeln(List).
:- halt.
```

```sh
$ swipl multi.pl
[dog,cat]
```

`extends(X, animal)`という1つの問い合わせに対して、`extends(dog, animal).`と`extends(cat, animal).`という2つの節がどちらも一致するので、2つの解が得られます。3章で「正体は後で明かす」としていた`between/3`も、実はこの仕組みだけで作れます。

```prolog
% my_between.pl
my_between(Low, High, Low) :- Low =< High.
my_between(Low, High, X) :-
    Low < High,
    Next is Low + 1,
    my_between(Next, High, X).

:- findall(X, my_between(1, 3, X), List), writeln(List).
:- halt.
```

```sh
$ swipl my_between.pl
[1,2,3]
```

`my_between/3`には2つの節があります。
1つ目は「LowがHigh以下なら、Xとして自分自身（Low）を返す」、2つ目は「まだHighに届いていなければ1つ進めて自分自身を呼び直す」というものです。
`findall/3`がこの述語をバックトラックしながら何度も呼び直すことで、1, 2, 3という解が順に集まります。

3〜4章で使っていた`between/3`・`forall/2`・`findall/3`・`member/2`・`append/3`・`select/3`は、どれも特別な構文ではなく、事実とルールの組み合わせでできた**ただの述語**でした。「ループに見えたものの正体は再帰する述語とバックトラックだった」というのが、この章までの伏線の答えです。
