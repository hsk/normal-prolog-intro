---
title: "9. DCG：パーサ作りに便利な道具（実は他にも使える）"
---

## 9. DCG：パーサ作りに便利な道具（実は他にも使える）

### 素朴なパーサ：リストの引き継ぎが面倒

7章で見た通り、Prologのリストは`[H|T]`に分解できます。この仕組みだけで、簡単な構文解析（パーサ）も書けます。試しに、`a`がいくつか並んだリストを認識するパーサを書いてみます。

```prolog
% parser_naive.pl
aa([a|T0], T) :- aa(T0, T).
aa(T, T).

:- ( aa([a, a, a], []) -> writeln(yes) ; writeln(no) ).
:- halt.
```

```sh
$ swipl parser_naive.pl
yes
```

`aa(In, Out)` は「Inの先頭から`a`を好きなだけ読み進めて、残りがOutになる」という関係です。`aa([a, a, a], [])` は「`[a,a,a]`を全部`a`として読み進めたら、残りは`[]`になる」ので成功します。

問題は、これを組み合わせて使おうとしたときです。「`a`の並びのあとに`b`の並びが続く」というパーサを書いてみます。

```prolog
% parser_naive2.pl
aa([a|T0], T) :- aa(T0, T).
aa(T, T).

bb([b|T0], T) :- bb(T0, T).
bb(T, T).

aabb(In, Out) :- aa(In, T1), bb(T1, Out).

:- ( aabb([a, a, b, b, b], []) -> writeln(yes) ; writeln(no) ).
:- halt.
```

```sh
$ swipl parser_naive2.pl
yes
```

`aabb/2`の中で、`aa/2`が読み終えた残り`T1`を、そのまま`bb/2`の入力として渡しています。パーサを1つ増やすたびに、この「残りのリストを次に引き継ぐ」ための変数（ここでは`T1`）を自分で用意して配線しなければなりません。組み合わせが増えるほど、この配線だけのコードが増えていきます。

### DCGとは：`-->`で書ける文法規則

この「残りのリストを引き継ぐ」という面倒な配線を、Prologが裏側で自動的にやってくれる仕組みが**DCG（Definite Clause Grammars）**です。書き方は`:-`の代わりに`-->`を使うだけです。

```prolog
% dcg.pl
aa --> [a], aa.
aa --> [].

bb --> [b], bb.
bb --> [].

aabb --> aa, bb.

:- ( phrase(aabb, [a, a, b, b, b]) -> writeln(yes) ; writeln(no) ).
:- halt.
```

```sh
$ swipl dcg.pl
yes
```

`aa --> [a], aa.` は「`a`という1要素を読んで、続けて`aa`を読む」という規則です。
`aabb --> aa, bb.`では、`aa`の残りを`bb`に手渡す配線は一切書いていません。
それでも`aa`のあとに`bb`が続けて読まれます。DCGの規則を呼び出すには、`phrase(Goal, List)`という組み込み述語を使います。

実は`aabb`は裏側で「読む前のリスト」と「残りのリスト」を引数に持つ普通の述語に変換されているだけなので、`aabb([a, a, b, b, b], [])`のように直接呼ぶこともできます。

```prolog
:- ( aabb([a, a, b, b, b], []) -> writeln(yes) ; writeln(no) ).
```

教科書的には`phrase/2`を使うべきだとは思いますが、筆者は`phrase/2`という名前を覚えておらず、こちらの「直接2引数で呼ぶ」書き方しか普段は使っていなかったりします。`phrase/2`は「残りのリストは全部読み切った（`[]`になる）はず」という場合に`[]`を省略できる分、少し書きやすくなる程度の違いです。

少し脱線しますが、似た話として`assertion/1`にも触れておきます。`assertion(1=1)`と書いても、単に`1=1`と書いても、成り立つかどうかを確認するという意味では同じです。違いは失敗したときの挙動で、`1=2`のようにただ失敗するゴールは（呼び出し側の書き方次第で）静かに失敗するだけですが、`assertion(1=2)`は失敗した時点でエラーとして例外を投げ、スタックトレース付きで「どこで何が成り立たなかったか」を報告してくれます。ゴールを1つ挟むだけで、その場に簡易的なテストのアサーションを仕込めるようなイメージです。

### 正体：`phrase/2`と差分リスト

DCGは特別な機能ではなく、先ほどの素朴なパーサに戻すための**構文変換（シンタックスシュガー）**です。SWI-Prologには、DCGの規則がどんな述語に変換されるかを確認する`listing/1`があります。

```prolog
% dcg_listing.pl
aa --> [a], aa.
aa --> [].

:- listing(aa//0).
:- halt.
```

```sh
$ swipl dcg_listing.pl
aa([a|A], B) :-
    aa(A, B).
aa(A, B) :-
    A=B.
```

`aa --> [a], aa.` は `aa([a|A], B) :- aa(A, B).` に変換されています。これは、まさに最初に自分で書いた`aa/2`と同じ形です。DCGの`Nonterminal`は、実際には「読む前のリスト」と「読んだ後の残りのリスト」という2つの引数を持つ普通の述語（`aa/2`）にすぎず、規則をつなげる`,`が、その2つの引数を自動で配線してくれているだけです。この「先頭のリストと残りのリストの組」を**差分リスト**と呼びます。

### 応用：足し算の式をパースする

DCGの規則は普通の述語なので、パースしながら値を組み立てていくこともできます。数値と`+`が交互に並んだ入力から、`1+2+3`のような複合項（構文木）を組み立ててみます。

```prolog
% add_parser.pl
expr(E) --> factor(T), expr_rest(T, E).
expr_rest(Acc, E) --> [+], factor(T), { E1 = Acc+T }, expr_rest(E1, E).
expr_rest(E, E) --> [].

factor(N) --> [N], { number(N) }.

:- phrase(expr(E), [1, +, 2, +, 3]), writeln(E).
:- halt.
```

```sh
$ swipl add_parser.pl
1+2+3
```

`{ ... }`で囲んだ部分は「リストを読み進めない、普通のPrologのゴール」を表します。`expr_rest(Acc, E)`は、それまでに組み立てた式を`Acc`として引き継ぎながら、`+`が続く限り式を継ぎ足していき、`+`が続かなくなったところで`E`として確定させます。「入力リストを引き継ぎながら、途中の値も一緒に運んで最後に取り出す」というこの書き方は、他の言語でいうモナド的な処理のつなげ方に近いものです。ここで組み立てた`1+2+3`という項は、次の章でそのまま計算できます。

---

[← 8. ループの書き方](8.md) | [10. term_expansion：ソースコードを書き換えるマクロ →](10.md)

[目次に戻る](README.md)
