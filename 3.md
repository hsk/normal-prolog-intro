---
title: "3. 単一化：一度だけ束縛できる参照"
---

## 3. 単一化：一度だけ束縛できる参照

### `=` は代入ではなく「等式の解決」

2章で「Prologの変数は空欄だ」と説明しましたが、その空欄に値を当てはめる仕組みには名前があります。**単一化（Unification）**です。

```prolog
% unify1.pl
:- X = 1, writeln(X).
:- halt.
```

```sh
$ swipl unify1.pl
1
```

`=`は「左辺に右辺を代入する」のではなく、「左辺と右辺が同じものになるように解決する」演算子です。今回はXが空欄なので、Xに1を当てはめれば両辺が一致し、成功します。

```prolog
% unify2.pl
:- X = 1, X = 2, writeln(X).
:- halt.
```

```sh
$ swipl unify2.pl
Warning: unify2.pl:1:
Warning:    Goal (directive) failed: user:(_A=1,_A=2,writeln(_A))
```

一度Xに1が当てはまると、Xはもう「1という値そのもの」です。続けて`X = 2`を試みても、1と2は一致しないので単一化は失敗します。ここが「箱」とは違うところで、Prologの変数は**一度だけ値を束縛できる参照**であり、書き換えることはできません（そのかわり、バックトラックすれば束縛は取り消されます。これは次章以降、必要になったところで少しずつ触れていきます）。

### 変数どうしの単一化

`=`の両辺がどちらも変数でも構いません。

```prolog
% unify3.pl
:- X = Y, Y = 1, writeln(X).
:- halt.
```

```sh
$ swipl unify3.pl
1
```

`X = Y`の時点では、XとYはまだどちらも空欄です。ここでPrologは「XとYを同じ空欄にする」という単一化を行います。あとから`Y = 1`とすると、XとYは同じものなので、Xを見ても1になっています。

この章で押さえておきたいのは、次の1点だけです。

- `=`は代入ではなく単一化であり、Prologの変数は一度だけ束縛できる参照である。

単一化は、これから先の章（複合項のパターンマッチ、述語の呼び出し、リストの逆算など）のいたるところに顔を出します。難しく考えず、「まだ何も決まっていない空欄に、矛盾なく値を当てはめる仕組み」とだけ覚えて先に進みましょう。

---

[← 2. 変数と条件分岐](2.md) | [4. 複合項（構造体） →](4.md)

[目次に戻る](README.md)
