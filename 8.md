---
title: "8. ループの書き方"
---

## 8. ループの書き方

### 反復処理：`between/3` と `forall/2`

```prolog
% forall.pl
:- forall(between(1, 3, X), writeln(X)).
:- halt.
```

```sh
$ swipl forall.pl
1
2
3
```

今の段階では、`forall(between(1, 3, X), writeln(X))` は「Xを1から3まで動かしながらwritelnする」という便利なfor文だと思ってください。
`between(1, 3, X)`が`1..3`のようなレンジ、`forall`がそのレンジを回すforループ、というイメージです。

### 集約：`findall/3`

ループの結果を1つずつ表示するだけでなく、リストとしてまとめて取り出したいこともあります。そこで使うのが`findall/3`です。

```prolog
% findall.pl
:- findall(X, between(1, 3, X), List), writeln(List).
:- halt.
```

```sh
$ swipl findall.pl
[1,2,3]
```

`findall(Template, Goal, List)` は「`Goal`を満たすすべての解について`Template`を集めて`List`にする」という意味です。`between/3`が1つずつ返す解を、`findall/3`が全部回収してリストにまとめています。

`findall/3`と未束縛の変数を組み合わせれば、7章の`member/2`や`select/3`からも同じように解を集められます。

```prolog
% member2.pl
:- findall(X, member(X, [1, 2, 3]), List), writeln(List).
:- halt.
```

```sh
$ swipl member2.pl
[1,2,3]
```

`member(X, List)`は「XがListの要素である」という関係でしたが、Xを未束縛にして`findall/3`で全部回収すると、Listの要素が1つずつ出てきます。「要素かどうかを判定する」用途にも「要素を1つずつ取り出す」用途にも使えるのは、同じ1つの関係を、どちらの向きに使うかだけの違いです。`select/3`も同様です。

```prolog
% select2.pl
:- findall(X-Rest, select(X, [1, 2, 3], Rest), Pairs), writeln(Pairs).
:- halt.
```

```sh
$ swipl select2.pl
[1-[2,3],2-[1,3],3-[1,2]]
```

リストから要素を1つずつ抜き出したときの「抜き出した要素」と「残り」の組み合わせを、すべて列挙できました。

### 応用：偶数だけ出力する

`findall/3`の`Goal`部分には、`,`で複数のゴールをつなげられます。条件を絞り込むゴールを挟んでおけば、条件に合うものだけを集められます。

```prolog
% evens.pl
:- findall(X, ( between(1, 10, X), 0 is X mod 2 ), List), writeln(List).
:- halt.
```

```sh
$ swipl evens.pl
[2,4,6,8,10]
```

`between(1, 10, X), 0 is X mod 2` は「Xを1から10まで動かし、かつXが偶数である」という意味です。`0 is X mod 2`が成り立たない（奇数の）Xはそこで失敗するので、`findall/3`はその値を集めません。結果として、偶数だけが集まったリストが得られます。

SQLに馴染みがあれば、`findall(Template, (Cond1, Cond2, ...), List)`は`SELECT Template FROM ... WHERE Cond1 AND Cond2`にかなり近い形だと感じるかもしれません。`between(1, 10, X)`が母集団を用意する`FROM`、`0 is X mod 2`が絞り込む`WHERE`、`Template`に指定した`X`が取り出す`SELECT`に対応しています。`findall/3`は、Prologにとってのちょっとした問い合わせ（クエリ）の書き方だと思うと理解しやすいかもしれません。

### 応用：FizzBuzz

5章の「戻り値ではなく引数への書き込み」という発想と、6章のパターンマッチを組み合わせれば、お馴染みのFizzBuzzも書けます。

```prolog
% fizzbuzz.pl
fizzbuzz(N, V) :-
    ( 0 is N mod 15 -> V = 'FizzBuzz'
    ; 0 is N mod 3 -> V = 'Fizz'
    ; 0 is N mod 5 -> V = 'Buzz'
    ; V = N
    ).

:- findall(V, ( between(1, 15, N), fizzbuzz(N, V) ), List), writeln(List).
:- halt.
```

```sh
$ swipl fizzbuzz.pl
[1,2,Fizz,4,Buzz,Fizz,7,8,Fizz,Buzz,11,Fizz,13,14,FizzBuzz]
```

`fizzbuzz(N, V)`は「15の倍数ならVは`FizzBuzz`、3の倍数ならVは`Fizz`、5の倍数ならVは`Buzz`、それ以外ならVはNそのもの」という関係を表す述語です。結果を`writeln`で直接出力せず、Vという引数に書き込ませているので、`findall/3`の`Template`にVを指定するだけで、変換結果を1つのリストとして集められます。

### 正体：ループに見えたものは、ただの述語だった

`between/3`・`forall/2`・`findall/3`は、一見すると`for`や`while`のような専用のループ構文に見えます。しかし5章で述語の作り方を学んだ今なら、その正体を種明かしできます。`between/3`は、実は事実とルールの組み合わせだけで自分で書けてしまいます。

```prolog
% my_between.pl
my_between(Low, High, Low) :- Low =< High.
my_between(Low, High, X) :-
    Low < High,
    Next is Low + 1,
    my_between(Next, High, X).

:- findall(X, my_between(1, 3, X), List), writeln(List).
:- halt.
```

```sh
$ swipl my_between.pl
[1,2,3]
```

`my_between/3`には2つの節があります。1つ目は「LowがHigh以下なら、Xとして自分自身（Low）を返す」、2つ目は「まだHighに届いていなければ1つ進めて自分自身を呼び直す」というものです。
`findall/3`がこの述語をバックトラックしながら何度も呼び直すことで、1, 2, 3という解が順に集まります。

`between/3`だけでなく、この章の`forall/2`・`findall/3`も、7章の`member/2`・`append/3`・`select/3`も、どれも特別な構文ではなく、事実とルールの組み合わせでできた**ただの述語**でした。「ループに見えたものの正体は、再帰する述語とバックトラックだった」というのが、この本の前半で見てきた「不思議」の答えです。

---

[← 7. リスト操作の基礎](7.md) | [9. DCG：パーサ作りに便利な道具（実は他にも使える） →](9.md)

[目次に戻る](README.md)
