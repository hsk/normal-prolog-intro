---
title: "8. 単一化（Unification）：Prologの真髄"
---

## 8. 単一化（Unification）：Prologの真髄

### 本質：`=` は代入ではなく「等式の解決」

```prolog
% unify1.pl
:- X = 1, writeln(X).
:- halt.
```

```sh
$ swipl unify1.pl
1
```

2章では「Xという空欄に1を当てはめる」とだけ説明しましたが、正体は**単一化（Unification）**です。`=`は左辺と右辺を「同じものにする」ための等式で、両辺が矛盾なく一致するように変数へ値を当てはめます。片方が変数ならその変数に値を当てはめ、両方とも複合項なら5章のパターンマッチのように形を照合し、対応する引数どうしを再帰的に単一化します。

```prolog
% unify2.pl
:- point(1, Y) = point(X, 2), writeln(X), writeln(Y).
:- halt.
```

```sh
$ swipl unify2.pl
1
2
```

左辺の`point(1, Y)`と右辺の`point(X, 2)`は、どちらも一部が変数です。
Prologは「同じ`point/2`という形をしていて、対応する引数どうしが矛盾なく一致するか」を確認しながら、`X = 1`、`Y = 2`という2つの単一化を同時に解いています。「代入」ではなく「等式を解いて両辺を一致させている」というのがここでのポイントです。

### 逆算の威力：方程式を自動解決する

普通の代入なら`X = Y + 1`のように右辺に未知数が残っていては使えませんが、単一化は「まだ決まっていない側から逆に決める」ことも自然にこなします。4章の`append/3`の逆算は、まさにこの単一化の応用でした。

```prolog
% unify3.pl
:- append(X, [c], [a, b, c]), writeln(X).
:- halt.
```

```sh
$ swipl unify3.pl
[a,b]
```

`append/3`自身は事実とルールの組み合わせでできた普通の述語です（6章参照）。
その内部で行われている処理も、結局は「`append(X, [c], [a, b, c])`という形を満たす`X`を求めるために、リストの中身を1つずつ`[H|T]`として単一化していく」だけです。「関数に入力を渡して出力を計算する」のではなく、「関係を表す式全体が矛盾なく成り立つように、まだ決まっていない部分を逆算で埋める」という発想が、単一化の威力です。

### まとめ：なぜ変数は「空欄」だったのか

2章で「Prologの変数は値を格納する箱ではなく、未知の空欄（プレースホルダ）だ」と説明したのは、ここまでの章すべてが単一化の上に成り立っていたからです。

- 2章の`X = 1`は、空欄`X`と`1`を単一化していた。
- 4章の`[H|T] = [1, 2, 3]`は、リストの構造を単一化してHとTに値を当てはめていた。
- 5章の`point(A, B) = point(1, 2)`は、複合項どうしを単一化してAとBに値を当てはめていた。
- 6章の「未束縛の変数を渡して結果を受け取る」書き方は、述語の中で行われる単一化によって、呼び出し側の変数にも値が見えるようになる仕組みだった。

もし変数が「値を格納する箱」だったなら、一度きりの代入しかできず、逆算も、パターンマッチも、リストの分解もできませんでした。変数が「まだ何も決まっていない空欄」だからこそ、Prologは等式を両方向から解き、事実とルールを照合し、パターンマッチで複合項を分解し、そして時には答えを逆算する、という論理プログラミングらしい振る舞いができるのです。単一化こそが、この本の1章から7章までで見てきたすべての「不思議」の共通の正体でした。

---

[← 7. パターンマッチによる分岐（switch文）](7.md) | [9. DCG：パーサ作りに便利な道具（実は他にも使える） →](9.md)

[目次に戻る](README.md)
