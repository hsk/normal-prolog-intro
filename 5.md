---
title: "5. 複合項（構造体）"
---

## 5. 複合項（構造体）

### 複合項とは：名前（functor）と複数のデータ

```prolog
% compound.pl
:- X = point(1, 2), writeln(X).
:- halt.
```

```sh
$ swipl compound.pl
point(1,2)
```

`point(1, 2)` のように「名前（functor）に複数のデータをぶら下げたもの」を**複合項**と呼びます。
名前と引数の個数の組み合わせを`point/2`のように表し、この引数の個数を**アリティ（arity）**と呼びます。
`point(1, 2)`と`point(1, 2, 3)`はどちらも`point`という名前ですが、アリティが違うので別物として扱われます。

実は2章で使っていたアトム（`abc`など）も、4章のリスト（`[1, 2, 3]`）も、複合項の仲間です。
アトムはアリティ0の複合項、リストは`'[|]'(H, T)`という2引数の複合項（HeadとTailを持つ、いわゆるLispの`cons`のようなもの）が入れ子になったものです。つまりPrologの「値」はすべて、アトム・数値・変数・複合項のどれかであり、複合項は構造体のようにもリストのようにも使える万能の入れ物です。

### データ分解：複合項のパターンマッチング

```prolog
% match.pl
:- X = point(1, 2), X = point(A, B), writeln(A), writeln(B).
:- halt.
```

```sh
$ swipl match.pl
1
2
```

`X = point(A, B)` は、`X`の中身である`point(1, 2)`と`point(A, B)`という形を照合し、`A`に1、`B`に2を当てはめています。2章で見た単純な変数への代入と同じ「単一化」の仕組みですが、複合項が相手だと「形が一致するか」まで同時にチェックされ、一致した部分の変数に値が埋まります。

### 分解の別解：`=..` によるリストとの相互変換

```prolog
% univ.pl
:- point(1, 2) =.. List, writeln(List).
:- halt.
```

```sh
$ swipl univ.pl
[point,1,2]
```

`=..`（univと読みます）は、複合項を「functor名+引数」のリストに変換します。逆にリストから複合項を組み立てることもできます。

```prolog
% univ2.pl
:- X =.. [point, 3, 4], writeln(X).
:- halt.
```

```sh
$ swipl univ2.pl
point(3,4)
```

`point(A, B) = point(1, 2)`のようにfunctor名が分かっている前提のパターンマッチとは違い、`=..`はfunctor名や引数の個数が実行時にしか決まらない場合に使えます。

### DSLとしての活用：演算子定義による独自の構文生成

複合項は`f(X, Y)`という書き方だけでなく、**演算子**として定義すれば中置記法でも書けます。

```prolog
% op.pl
:- op(700, xfx, extends).

:- X = (dog extends animal), writeln(X).
:- halt.
```

```sh
$ swipl op.pl
extends(dog,animal)
```

`:- op(700, xfx, extends).` は「`extends`という名前を、優先順位700・中置（xfx）の演算子として使えるようにする」というディレクティブです。
`dog extends animal`と書いても、中身は`extends(dog, animal)`という普通の複合項でしかありません。このように、複合項に見た目だけ人間に読みやすい構文を被せられるのがPrologの演算子定義で、たとえば「継承関係を宣言するDSL」のような、特定の分野に特化した記法を作るときの土台になります。

実はここまで使ってきた`:-`や`,`、`=`、`is`、`+`なども、すべて最初から`op/3`で定義済みの演算子です。代表的なものの優先順位（数字が大きいほど結合が弱い＝外側になる）を挙げておきます。

| 演算子 | 優先順位 | 型 |
| --- | --- | --- |
| `:-`（規則） | 1200 | xfx |
| `;` | 1100 | xfy |
| `->` | 1050 | xfy |
| `,` | 1000 | xfy |
| `\+` | 900 | fy |
| `=`, `is`, `==` | 700 | xfx |
| `+`, `-`（2項） | 500 | yfx |
| `*`, `/` | 400 | yfx |

自分で定義する`extends`の700という値も、`=`や`is`と同じ優先順位に合わせたものです。組み込みの演算子の一覧と正確な優先順位は、SWI-Prologのドキュメントにまとまっています。

- [SWI-Prolog: Operators](https://www.swi-prolog.org/pldoc/man?section=operators)

---

[← 4. リスト操作の基礎](4.md) | [6. 述語（述語論理としての関数）を作ろう →](6.md)

[目次に戻る](README.md)
