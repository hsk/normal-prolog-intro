---
title: "10. term_expansion：ソースコードを書き換えるマクロ"
---

## 10. term_expansion：ソースコードを書き換えるマクロ

### 題材：足し算の評価器

9章のDCGパーサは`1+2+3`のような複合項を組み立てるところまでで終わっていました。この項を実際に計算するための評価器を、まずは普通のPrologで書いておきます。

```prolog
% eval.pl
eval(I, I) :- integer(I).
eval(E1+E2, I) :- eval(E1, I1), eval(E2, I2), I is I1 + I2.

:- eval(1+2+3, R), writeln(R).
:- halt.
```

```sh
$ swipl eval.pl
6
```

`eval(I, I) :- integer(I).` は「Iが整数ならそのまま評価結果もIである」、`eval(E1+E2, I) :- ...` は「`E1+E2`の評価結果は、E1とE2をそれぞれ評価して足したもの」という規則です。この`eval/2`を、日本語の構文で書けるようにしてみます。

### 演算子だけでは足りない

5章で見た`op/3`を使えば、`ならば`という日本語の中置演算子を定義できます。
「AならばB」は「Aが成り立つならBが成り立つ」という意味なので、`eval(...) :- ...`と対応させるには、条件（Body）を左、結論（Head）を右に置きます。

```prolog
% jp_op_only.pl
:- op(1200, xfx, ならば).

整数(I) :- integer(I).

整数(I) ならば 評価(I, I).
( 評価(E1, I1), 評価(E2, I2), I is I1 + I2 ) ならば 評価(E1+E2, I).

:- 評価(1+2+3, R), writeln(R).
:- halt.
```

```sh
$ swipl jp_op_only.pl
ERROR: jp_op_only.pl:8:
ERROR:    '<meta-call>'/1: Unknown procedure: 評価/2
```

`整数(I) ならば 評価(I, I).` という行は構文としては読み込めますが、中身は`ならば(整数(I), 評価(I, I))`という**ただの複合項**でしかありません。Prologが規則として認識するのは`:-/2`という決まった形だけなので、`評価/2`という述語はどこにも定義されないままです。5章の`extends`のような「値としての複合項」と、6章で定義した「実際に呼び出せる述語」との違いがここに出ています。

### term_expansionとは：読み込み時にコードを書き換えるマクロ

この`ならば(Body, Head)`という形を、Prologが理解できる`Head :- Body`に変換してしまえば解決します。それを行うのが**`term_expansion/2`**です。

```prolog
% jp_op.pl
:- op(1200, xfx, ならば).

term_expansion((Body ならば Head), (Head :- Body)).

整数(I) :- integer(I).

整数(I) ならば 評価(I, I).
( 評価(E1, I1), 評価(E2, I2), I is I1 + I2 ) ならば 評価(E1+E2, I).

:- 評価(1+2+3, R), writeln(R).
:- halt.
```

```sh
$ swipl jp_op.pl
6
```

`term_expansion(In, Out)` は「ソースコードを読み込むとき、`In`という形の項を見つけたら`Out`に置き換えてから登録する」というルールです。
SWI-Prologはファイルを1つの節（term）ごとに読み込みますが、そのたびに`term_expansion/2`が定義されていないか確認し、あれば変換してから普段どおり処理します。
`整数(I) ならば 評価(I, I).`は、読み込まれた瞬間に`評価(I, I) :- 整数(I).`という普通の規則に変換され、これでようやく`評価/2`が呼び出せる述語として登録されます。

`op/3`は「読みやすい**見た目**」を作るための仕組み、`term_expansion/2`は「その見た目を実際に**動く**Prologのコードへ変換する」ための仕組みです。この2つを組み合わせることで、`ならば`のような日本語の構文がそのまま動くプログラムになります。

### さらに発展：自然言語に近づける

`ならば`だけでなく、`が`・`は`・`かつ`・`を評価すると`といった助詞や語句もすべて演算子として定義してしまうと、評価器の定義そのものを、かなり自然な日本語の文として書けます。

```prolog
% nl.pl
:- op(1200, xfx, ならば).
:- op(1000, xfy, かつ).
:- op(700, xfx, が).
:- op(700, xfx, は).
:- op(700, xfx, を評価すると).

term_expansion((Body ならば Head), (Head :- Body)).

A, B ならば A かつ B.
integer(I) ならば I が 整数.
Val is Expr ならば Expr は Val.

I が 整数 ならば I を評価すると I.
E1 を評価すると I1 かつ E2 を評価すると I2 かつ I1 + I2 は I ならば E1+E2 を評価すると I.

:- (1+2+3) を評価すると R, writeln(R).
:- halt.
```

```sh
$ swipl nl.pl
6
```

2つ目の規則は「E1を評価するとI1、かつE2を評価するとI2、かつI1+I2はIならば、E1+E2を評価するとI」と、ほぼそのまま日本語として読み下せます。
面白いのは、`かつ`・`が`・`は`の中身自体も、普通の`:-`ではなく同じ`ならば`で定義している点です。`integer(I) ならば I が 整数.`は「Iが整数であるならば、Iが整数である」というほぼ同語反復に見えますが、左辺は組み込み述語`integer/1`の呼び出し、右辺は自分で定義した演算子`が`の呼び出しなので、「素のPrologの言葉」を「日本語の言葉」に翻訳する対訳表のような役割を果たしています。
`A, B ならば A かつ B.`も同様に、Prologの`,`を`かつ`という言葉に翻訳しています。

述語論理は本来、主語・述語・関係をそのまま文として書けるように作られた体系です。`op/3`と`term_expansion/2`を組み合わせると、Prologはその出自に立ち返って、数式を自然言語の文法にほぼそのまま埋め込めることが分かります。

「日本語プログラミング」と聞くと、ネタや奇抜な実験のように感じるかもしれません。しかし、ここまでの章を読んできた人なら分かる通り、Prologの規則はもともと「XならばY」「XはYである」「XがYを満たす」といった、自然言語の文とほぼ同じ形をした論理式です。
6章の`extends(dog, animal)`も、5章の`dog extends animal`という中置記法も、結局は「dogはanimalを継承する」という日本語の文をそのままデータとして表現していただけでした。

論理プログラミングに慣れてくると、述語を「主語と述語の関係」として読み書きするのがむしろ自然になってくるので、そこに`op/3`と`term_expansion/2`で言葉を足しているだけのこの例は、奇抜というよりは論理プログラミングの本来の姿に一歩近づいているだけとも言えます。

ただし、これはあくまで「ここまで自由にできる」という実演です。実際のプロジェクトでここまで作り込むと、次の注意点がより重くのしかかってくる点は忘れないでください。

### 注意点

`term_expansion/2`はソースコード全体に対して働くマクロなので、強力な分だけ副作用も大きくなります。

- 変換ルールを増やすほど、「実際に何が実行されているか」がソースコードの見た目から想像しにくくなる。
- `term_expansion/2`自体のバグは、変換対象のコード全体に影響する。
- 他のライブラリも`term_expansion/2`を使っていることがあり、変換の順序次第で意図しない干渉が起きることがある。

DCG（9章）の`-->`も、実はSWI-Prologが内部的に用意している`term_expansion`の一種です。「構文を変えれば書きやすくなる」という発想は共通していますが、自分で`term_expansion/2`を定義するときは、チーム開発などでは特に、この「見た目と動作のズレ」がコストになることを意識しておくとよいでしょう。

---

[← 9. DCG：パーサ作りに便利な道具（実は他にも使える）](9.md)

[目次に戻る](README.md)
