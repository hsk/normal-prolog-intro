---
title: "4. リスト操作の基礎"
---

## 4. リスト操作の基礎

普通のプログラミング言語では配列（固定長の連続領域）とリスト（連結リスト）が別物として使い分けられますが、Prologには配列にあたるデータ構造がなく、複数の値をまとめるときは基本的に**リスト**を使います。添字で`O(1)`アクセスしたい場面では別途`array`ライブラリなどもありますが、この入門ではまずリストの扱いに慣れることを優先します。

### 構造：`[H|T]`（HeadとTail）

2章で見た`[1, 2, 3]`は、実は`[H|T]`という形の糖衣構文です。`H`（Head）が先頭の要素、`T`（Tail）が残りのリストを表します。

```prolog
% headtail.pl
:- [H|T] = [1, 2, 3], writeln(H), writeln(T).
:- halt.
```

```sh
$ swipl headtail.pl
1
[2,3]
```

`T`である`[2, 3]`も同じ形で分解できるので、`[1, 2, 3]`は`[1|[2|[3|[]]]]`という入れ子として表現されています。`[]`は空リストです。この「先頭と残り」に分解できる構造こそが、リストが**再帰的データ構造**と呼ばれる理由です。長さを数えるには、この構造を自分でたどらなくても組み込みの`length/2`が使えます。

```prolog
% length.pl
:- length([1, 2, 3], N), writeln(N).
:- halt.
```

```sh
$ swipl length.pl
3
```

### 基本操作：`member/2` と `append/3` の順算・逆算

```prolog
% member.pl
:- ( member(2, [1, 2, 3]) -> writeln(yes) ; writeln(no) ).
:- halt.
```

```sh
$ swipl member.pl
yes
```

`member(X, List)` は「XがListの要素である」という関係です。ここまでは配列の`in`演算子のような使い方ですが、Prologの`member/2`は**Xが未束縛でも使えます**。

```prolog
% member2.pl
:- findall(X, member(X, [1, 2, 3]), List), writeln(List).
:- halt.
```

```sh
$ swipl member2.pl
[1,2,3]
```

「XはListの要素である」という同じ1つの関係が、「要素かどうかを判定する」用途にも「要素を1つずつ取り出す」用途にも使えています。`append/3`も同様です。

```prolog
% append.pl
:- append([1, 2], [3, 4], List), writeln(List).
:- halt.
```

```sh
$ swipl append.pl
[1,2,3,4]
```

ここまでは「2つのリストを連結する」という順算の使い方です。しかし`append/3`は引数のどこを埋めるかを変えるだけで、**逆算**にも使えます。

```prolog
% append2.pl
:- append(X, [3, 4], [1, 2, 3, 4]), writeln(X).
:- halt.
```

```sh
$ swipl append2.pl
[1,2]
```

「Xと`[3,4]`をつなげたら`[1,2,3,4]`になる、そんなXは何か」を、Prologが自動で解いてくれています。関数のように「入力から出力を計算する」だけでなく、「関係を満たす値を逆から求める」ことができるのが、Prologの述語の大きな特徴です。

### 要素抽出：`select/3`

```prolog
% select.pl
:- select(2, [1, 2, 3], Rest), writeln(Rest).
:- halt.
```

```sh
$ swipl select.pl
[1,3]
```

`select(X, List, Rest)` は「ListからXを1つ取り除いた残りがRestである」という関係です。`member/2`と同様に、Xを未束縛にして呼び出すこともできます。

```prolog
% select2.pl
:- findall(X-Rest, select(X, [1, 2, 3], Rest), Pairs), writeln(Pairs).
:- halt.
```

```sh
$ swipl select2.pl
[1-[2,3],2-[1,3],3-[1,2]]
```

リストから要素を1つずつ抜き出したときの「抜き出した要素」と「残り」の組み合わせを、すべて列挙できました。

`select/3`には、要素を置き換えられる4引数版`select/4`もあります。

```prolog
% select4.pl
:- select(2, [1, 2, 3], 20, List), writeln(List).
:- halt.
```

```sh
$ swipl select4.pl
[1,20,3]
```

`select(X, List1, Y, List2)` は「List1のXをYに置き換えたものがList2である」という関係です。`select/3`が「取り除く」だけだったのに対し、`select/4`は「取り除いた場所に別の値を入れる」までを1つの述語で行います。
